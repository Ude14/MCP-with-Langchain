# An MCP-Based DataSpace Agent Using LangChain

This repository contains everything you need to run:

- An **MCP Server**
- An **Ollama LLM Server**
- A **LangChain agent** that connects to the MCP server and allows you to interact with it.

Additionally, it explores different approaches to **reduce token consumption**, as this is one of the main limitations of the technology.

The project focuses on the design and implementation of an **efficient intelligent agent** capable of managing the **DataSpace** of the Universidad Politécnica de Madrid.


# 1. Running the Ollama Server

First, install **Ollama** from the official website:

https://ollama.com/download/windows

## Configure Ollama

Before installing or running any model, configure Ollama to listen on all network interfaces. This is required if external applications need to connect to it.

```bash
set OLLAMA_HOST=0.0.0.0:11434
```

## Start the Ollama server

```bash
ollama serve
```

## Download a model

Open a new terminal and choose a model from:

https://ollama.com/search

Then download it with:

```bash
ollama pull <model>
```

For example:

```bash
ollama pull llama3.2:1b
```

## Test the model

Run the downloaded model:

```bash
ollama run llama3.2:1b
```

You can ask the model any question to verify that everything works correctly. Exit the interactive session with:

```text
/bye
```

If the model cannot be found, verify that it was installed successfully:

```bash
ollama list
```

The command should display the installed models along with their sizes and other information.

Finally, make sure the Ollama server is running before using the rest of the project:

```bash
ollama serve
```

---

# 2. Running the MCP Server

## Install FastMCP

```bash
pip install fastmcp
```

## Run the server

You can start the MCP server using FastMCP:

```bash
fastmcp run server_mcp_lang.py:mcp --port 9001 --transport streamable-http
```

Alternatively, you can run it directly with Python:

```bash
python server_mcp.py
```

---

# 3. Running the LangChain Client

To test the MCP server using the provided LangChain client, simply run:

```bash
python clientLangChain.py
```

---

# 4. Running the Agent as an API Server

If you want to expose the LangChain agent through a REST API, make sure the following services are already running:

1. Ollama Server
2. MCP Server

Then start the API server:

```bash
uvicorn main_server:app --reload
```

Once the server is running, you can send requests using `curl`:

```bash
curl -X POST \
  -H "Content-Type: application/json" \
  -d "{\"user_prompt\":\"Give me the author of the book The Lord of the Rings\"}" \
  http://127.0.0.1:8000/answerer/
```

---

# Project Workflow

The complete execution flow is:

```text
User
   │
   ▼
LangChain Agent
   │
   ▼
MCP Server
   │
   ▼
Ollama LLM
```

The LangChain agent receives the user's request, communicates with the MCP server whenever tools are required, and the MCP server uses the Ollama LLM to generate the final response.

---

# Additional Information

## System Screenshots

The `images/` folder contains several screenshots showing the system in action. These images illustrate the complete workflow, including:

- The Ollama LLM running locally.
- The MCP server execution.
- The LangChain client interacting with the MCP server.
- Example responses generated by the agent.
- Information about token consuption

These screenshots can be useful for verifying that your setup is working as expected and for understanding the overall architecture.

---

## Connect to Claude

One of the main advantages of implementing the server using the **Model Context Protocol (MCP)** is that it can be connected not only to custom clients (such as the provided LangChain agent), but also to MCP-compatible applications like **Claude Desktop**.

To connect the MCP server to Claude Desktop:

1. Make sure your MCP server is running.
2. Locate the Claude Desktop configuration file (`claude_desktop_config.json`).
3. Add your MCP server configuration. For example:

```json
{
  "mcpServers": {
    "my-mcp-server": {
      "command": "python",
      "args": [
        "server_mcp.py"
      ]
    }
  }
}
```

Alternatively, if your server is exposed through HTTP using the **streamable-http** transport, configure Claude to connect to the corresponding endpoint according to the MCP transport you are using.

After restarting Claude Desktop, the tools exposed by your MCP server will automatically become available to the assistant.

### Benefits of using Claude with MCP

Connecting your MCP server to Claude Desktop provides several advantages:

- **Native tool integration:** Claude can automatically discover and invoke the tools exposed by your MCP server.
- **No client-side development required:** You can interact with your tools directly from Claude without writing a custom client.
- **Reusable architecture:** The same MCP server can be shared across multiple AI applications that support the MCP standard.
- **Vendor-independent protocol:** MCP is an open protocol, making your tools portable across different LLM providers and clients.
- **Easy extensibility:** Adding new tools to your server automatically makes them available to compatible MCP clients.
- **Local execution:** Your tools and data remain on your own machine, providing greater privacy and control.


### Optimized versions
